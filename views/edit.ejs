<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
     <h5>Edit your post</h5>
    <form action="/update/<%= post._id %>" method="post" class="bg-zinc-800 p-6 rounded-lg shadow-md">
      <div class="mb-4">
        <textarea id="post"name="content" rows="4" class="w-full p-2 mt-2 bg-zinc-700 border border-zinc-600 rounded-lg text-white"><%= post.content%></textarea>
      </div>
      <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">add post</button>
    </form>
  
  </div>
</body>
</html>